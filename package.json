{
  "name": "@codeperate/form-builder",
  "version": "0.0.1",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "del-cli dist && tsc -p tsconfig.build.json",
    "release": "pnpm run build && npx np --no-cleanup --no-release-draft"
  },
  "dependencies": {
    "@codeperate/simple-store": "^2.0.0",
    "@codeperate/utils": "^1.0.18",
    "@types/json-schema": "^7.0.11",
    "ajv": "^8.11.0",
    "ajv-formats": "^2.1.1",
    "array-move": "^4.0.0",
    "lit": "^2.4.1"
  },
  "devDependencies": {
    "typescript": "^4.6.4",
    "del-cli": "^5.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
